description: ESP32 USB Host UAC manager (installs the USB Audio Class client)

compatible: "espressif,esp32-usbhost-uac"

properties:
  _unused:
    type: int
    default: 0
    description: Placeholder required by the binding schema; this driver has no device-tree configuration.
