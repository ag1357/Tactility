#include <tactility/module.h>

extern "C" {

Module elecrow_crowpanel_basic_28_module = {
    .name = "elecrow-crowpanel-basic-28",
    .start = nullptr,
    .stop = nullptr,
    .drivers = nullptr,
    .symbols = nullptr,
    .internal = nullptr
};

}
