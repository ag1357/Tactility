#include <tactility/module.h>

extern "C" {

Module m5stack_cores3_module = {
    .name = "m5stack-cores3",
    .start = nullptr,
    .stop = nullptr,
    .drivers = nullptr,
    .symbols = nullptr,
    .internal = nullptr
};

}
