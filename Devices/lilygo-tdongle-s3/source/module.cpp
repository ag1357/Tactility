#include <tactility/module.h>

extern "C" {

Module lilygo_tdongle_s3_module = {
    .name = "lilygo-tdongle-s3",
    .start = nullptr,
    .stop = nullptr,
    .drivers = nullptr,
    .symbols = nullptr,
    .internal = nullptr
};

}
