#include <tactility/module.h>

extern "C" {

Module lilygo_tdeck_max_module = {
    .name = "lilygo-tdeck-max",
    .start = nullptr,
    .stop = nullptr,
    .drivers = nullptr,
    .symbols = nullptr,
    .internal = nullptr
};

}
