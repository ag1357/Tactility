#include <tactility/module.h>

extern "C" {

Module generic_esp32p4_module = {
    .name = "generic-esp32p4",
    .start = nullptr,
    .stop = nullptr,
    .drivers = nullptr,
    .symbols = nullptr,
    .internal = nullptr
};

}
